<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hublabdiv" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- 1. Widget Navigation Pill 4x1 -->
        <receiver
            android:name=".HubPillWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Barra de Navegação (4x1)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_pill_widget_info" />
        </receiver>

        <!-- 2. Widget Profile Avatar 1x1 -->
        <receiver
            android:name=".HubProfileWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Perfil &amp; Lab (1x1)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_profile_widget_info" />
        </receiver>

        <!-- 3. Widget Global (4x3) -->
        <receiver
            android:name=".HubGlobalWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Widget Global (4x3)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_global_widget_info" />
        </receiver>

        <!-- 4. Widget Calendário (3x3) -->
        <receiver
            android:name=".HubCalendarWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Calendário Mensal (3x3)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_calendar_widget_info" />
        </receiver>

        <!-- 5. Widget Agenda (2x3) -->
        <receiver
            android:name=".HubAgendaWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Agenda &amp; Tarefas (2x3)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_agenda_widget_info" />
        </receiver>

        <!-- 6. Widget Semanal (4x2) -->
        <receiver
            android:name=".HubWeeklyWidgetProvider"
            android:exported="true"
            android:label="HUB LabDiv - Visão Semanal (4x2)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_weekly_widget_info" />
        </receiver>

        <!-- Legacy Widget Fallback -->
        <receiver android:name=".HubWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/hub_widget_info" />
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
