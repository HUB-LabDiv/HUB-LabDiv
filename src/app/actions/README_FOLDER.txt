==================================================
PASTA: actions
DESCRIÇÃO: Sistema de roteamento do Next.js (App Router). Contém as páginas, layouts e componentes de servidor da aplicação.
==================================================

ARQUIVOS:
  - account.ts: Server Action (Lógica de servidor).
  - admin-notifications.ts: Server Action (Lógica de servidor).
  - admin.ts: Server Action (Lógica de servidor).
  - arena.ts: Server Action (Lógica de servidor).
  - auth.ts: Server Action (Lógica de servidor).
  - calendar.ts: Server Action (Lógica de servidor).
  - collections.ts: Server Action (Lógica de servidor).
  - comments.ts: Server Action (Lógica de servidor).
  - corrections.ts: Server Action (Lógica de servidor).
  - disciplines.ts: Server Action (Lógica de servidor).
  - drops.ts: Server Action (Lógica de servidor).
  - entanglements.ts: Server Action (Lógica de servidor).
  - feedback.ts: Server Action (Lógica de servidor).
  - follows.ts: Server Action (Lógica de servidor).
  - gamification.ts: Server Action (Lógica de servidor).
  - groups.ts: Server Action (Lógica de servidor).
  - knowledge.ts: Server Action (Lógica de servidor).
  - match.ts: Server Action (Lógica de servidor).
  - media.ts: Server Action (Lógica de servidor).
  - notes.ts: Server Action (Lógica de servidor).
  - notifications.ts: Server Action (Lógica de servidor).
  - playlists.ts: Server Action (Lógica de servidor).
  - profiles.ts: Server Action (Lógica de servidor).
  - progress.ts: Server Action (Lógica de servidor).
  - quiz.ts: Server Action (Lógica de servidor).
  - reactions.ts: Server Action (Lógica de servidor).
  - recommendations.ts: Server Action (Lógica de servidor).
  - reports.ts: Server Action (Lógica de servidor).
  - reset.ts: Server Action (Lógica de servidor).
  - submissions.ts: Server Action (Lógica de servidor).
